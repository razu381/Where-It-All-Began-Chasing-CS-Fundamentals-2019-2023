// && will return the first false value or last truthy value

let age = 177;
// let report_police = false;
// let door = "open";

// age >= 18 ? console.log("You can drink",door = "open") : (report_police = true, console.log("Drink grape juice, that's same as wine but without the bad stuff", door = "close"))

//if there no else then we can use this
age >= 18 ? console.log("hi") : null;
age >= 18 && console.log("hi");
